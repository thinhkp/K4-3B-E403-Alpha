[project]
name = "k4-3b-e403-alpha"
version = "0.1.0"
description = "VLearn Grounded Tutor — slide-grounded learning assistant"
readme = "RUN_VLEARN.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "openai>=1.60,<2",
    "pandas>=2.2,<3",
    "pymupdf>=1.24,<2",
    "pymongo>=4.10,<5",
    "python-dotenv>=1.0,<2",
    "qdrant-client>=1.13,<1.14",
    "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
# This repository is an application, not an installable Python package.
package = false
